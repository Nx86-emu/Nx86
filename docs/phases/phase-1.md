# Phase 1: Repository Bootstrap

Phase 1 creates the Rust workspace, crate skeletons, formatting and lint
configuration, CI placeholder, issue templates, docs folders, and basic logging
crate.

## Exit Criteria

- The workspace builds.
- The empty application launches.
- CI can compile the empty project on Ubuntu.
