# Phase 5: Job System and IPC Foundation

Phase 5 adds JSON-line IPC messages and smoke worker process modes for compiler
and runtime process supervision.

## Exit Criteria

- The GUI can launch a worker process.
- The worker sends progress events.
- The GUI displays progress and supports cancellation.
